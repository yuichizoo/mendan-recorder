import type { InterviewType, Judgment, Segment } from '../../types'
import { INTERVIEW_TYPE_LABELS, MEASURES, WORK_CLASS_LABELS } from '../../types'

const COMMON_RULES = `あなたは産業医の文書作成アシスタントです。産業医面談の音声入力メモ(セグメント式・誤変換を含みうる)から、企業提出用文書の下書きをプレーンテキストで作成します。

# 安全ルール(最重要)
- 創作禁止: メモにない医学的所見・数値・診断名・経過を創作しない。時間外労働時間数・血圧値・ストレスチェック点数などはメモに書かれた値のみ使用する。メモにない項目は空欄のままにする。
- 判断項目は産業医の医学的判断: 指導区分・就業区分・復職可否などは、メモから明確に読み取れる場合(「就業制限とした」「復職可と判断」等)のみ ☒ を付け、曖昧な場合はすべて ☐ のまま残す。勝手に埋めない。
- 個人情報: 氏名・従業員番号・生年月日は書かない(メモにあっても空欄)。所属・事業場名・年齢・性別は業務上必要なのでメモにあれば記載してよい。
- 音声誤変換: 文脈から明らかな誤変換は解釈して直す。医学的に意味が変わりうる語は原文のまま採用し、要確認リストに挙げる。
- 文体: 「である調」または体言止め。企業の人事担当者が読むため、専門用語には必要最小限の平易な補足(例:「SAS(睡眠時無呼吸症候群)の精査」)。診断名の詳細より就業上の影響と措置を中心に。各記述欄は2〜4行で簡潔に。
- 就業上の措置は事業者が実施可能な具体的措置として書く。「様子を見る」ではなく「時間外労働45時間/月以内」のように数値・行動レベルで。

# 出力形式
- プレーンテキストのみ。マークダウン記法・前置き・後書きは一切書かない。
- 選択肢は ☒(該当)/ ☐(非該当・未確定)で表す。
- 文書本文の後に1行「---要確認---」を置き、その下に産業医が確認すべき点(空欄にした判断項目、誤変換の解釈、メモから推定した箇所)を「・」の箇条書きで記す。`

const TEMPLATE_CHOKI = `# 文書テンプレート(この構成・項目順で出力)
■ 面接指導結果報告書(長時間労働者用)
実施年月日: {実施日}
面談時間: {面談時間}
対象者: ID: {対象者ID} / 所属: 　 / 性別: 　 / 年齢:
【勤務の状況】
(直近1か月の時間外労働時間、2〜6か月平均が分かれば併記。深夜業・出張・交替勤務などの負荷要因、業務内容)
【疲労蓄積の状況】
☐ 低い ☐ やや高い ☐ 高い ☐ 非常に高い
【その他の心身の状況】
☐ 所見なし ☐ 所見あり(　)
【指導区分】(複数可)
☐ 0:措置不要 ☐ 1:要保健指導 ☐ 2:要経過観察 ☐ 3:要再面接(時期:　) ☐ 4:現病治療継続又は医療機関紹介
【特記事項】

■ 就業上の措置に係る意見書
【就業区分】
☐ 0:通常勤務 ☐ 1:就業制限・配慮 ☐ 2:要休業
【労働時間の短縮】
☐ 指示なし ☐ 時間外労働の制限(　時間/月まで) ☐ 時間外労働の禁止 ☐ 就業時間の制限(　時　分〜　時　分) ☐ 変形労働時間制または裁量労働制の対象からの除外 ☐ 就業の禁止 ☐ その他(　)
【労働時間以外の措置】
☐ 就業場所の変更 ☐ 作業の転換 ☐ 深夜業の回数の減少 ☐ 昼間勤務への転換 ☐ その他
具体的内容:
【措置期間】
【医療機関への受診配慮等】
【その他連絡事項】
産業医: 佐々木 雄一`

const TEMPLATE_KOSTRESS = `# 文書テンプレート(この構成・項目順で出力)
■ 面接指導結果報告書(高ストレス者用)
実施年月日: {実施日}
面談時間: {面談時間}
対象者: ID: {対象者ID} / 所属: 　 / 性別: 　 / 年齢:
【勤務の状況】
(労働時間、ストレス要因(業務量/人間関係/裁量)、業務内容)
【ストレスチェック結果】
A(ストレスの要因): 　点 / B(心身の自覚症状): 　点 / C(周囲の支援): 　点
(点数はメモにあれば記載。なければ空欄のまま。点数の創作は禁止)
医学的所見に関する特記事項:
【その他の心身の状況】
☐ 所見なし ☐ 所見あり(　)
【指導区分】(複数可)
☐ 0:措置不要 ☐ 1:要保健指導 ☐ 2:要経過観察 ☐ 3:要再面接(時期:　) ☐ 4:現病治療継続又は医療機関紹介
【特記事項】

■ 就業上の措置に係る意見書
【就業区分】
☐ 0:通常勤務 ☐ 1:就業制限・配慮 ☐ 2:要休業
【労働時間の短縮】
☐ 指示なし ☐ 時間外労働の制限(　時間/月まで) ☐ 時間外労働の禁止 ☐ 就業時間の制限(　時　分〜　時　分) ☐ 変形労働時間制または裁量労働制の対象からの除外 ☐ 就業の禁止 ☐ その他(　)
【労働時間以外の措置】
☐ 就業場所の変更 ☐ 作業の転換 ☐ 深夜業の回数の減少 ☐ 昼間勤務への転換 ☐ その他
具体的内容:
【職場環境の改善に関する意見】
【措置期間】
【医療機関への受診配慮等】
【その他連絡事項】
産業医: 佐々木 雄一

# 法令上の注意(要確認リストに含めること)
高ストレス者面談の結果を事業者へ提供する際は本人同意の確認が必要な情報が含まれうる。意見書は就業上の措置に必要な範囲に限定する。`

const TEMPLATE_FUKUSHOKU = `# 文書テンプレート(この構成・項目順で出力)
■ 職場復帰に関する意見書
発行日: {実施日}
宛先: 　(会社名がメモにあれば「株式会社○○ 人事労務責任者」の形で)
対象者: ID: {対象者ID} / 事業場: 　 / 所属: 　 / 性別: 　 / 年齢:
【目的】
☐ 新規 ☐ 変更 ☐ 解除
【復職可否】
☐ 可 ☐ 条件付き可 ☐ 不可
【医学的意見】
(病名の詳細は書かず「療養により症状は改善し、通常の勤務時間帯での就労が可能な状態」のように就業能力ベースで2〜5行。生活リズム・通勤訓練・主治医の復職可診断書の有無はメモにある場合のみ)
【就業上の配慮】
時間外勤務: ☐ 禁止 ☐ 制限(　時間まで)
交替勤務: ☐ 禁止 ☐ 制限
休日勤務: ☐ 禁止 ☐ 制限
就業時間短縮: ☐ あり(遅刻・早退 　時間) ☐ なし
出張: ☐ 禁止 ☐ 制限
作業転換: ☐ あり(内容:　) ☐ なし
配置転換・異動: ☐ あり(内容:　) ☐ なし
その他:
今後の見通し:
(例: 1か月ごとに面談し、体調安定を確認しながら段階的に制限を解除する見込み)
【面談実施日】{実施日}(面談時間: {面談時間})
【措置期間】
産業医: 佐々木 雄一

# 記載の定石
復職直後は「時間外禁止・休日勤務禁止・出張禁止」+段階的解除の見通しをセットで書く(メモと矛盾しない範囲で)。措置期間は原則1〜3か月で区切る。主治医の診断書と産業医意見が食い違う場合はその旨を医学的意見欄に明記。`

const TEMPLATE_OTHER = `# 文書テンプレート(この構成・項目順で出力)
■ 産業医意見書・報告書
日付: {実施日}
事業所名: 　(メモにあれば)
対象者: ID: {対象者ID}(面談時間: {面談時間})
【現在の状況】
(面談で把握した事実(体調、勤務状況、通院状況)を客観的に2〜6行。本人の主観的訴えは「〜とのこと」「〜と述べている」で事実と区別)
【産業医見解】
(医学的評価と就業への影響。人事担当者が読んで行動できるレベルの具体性で2〜6行)
【就業上の措置に関する意見】
(内容がある場合のみ。なければこの見出しごと省略)
【今後のフォローアップ】
(内容がある場合のみ。なければこの見出しごと省略)
産業医: 佐々木 雄一

# 注意
病名・検査値は就業上の措置に必要な範囲に限定。本人に不利益となりうる情報が含まれる場合は要確認リストで本人同意の確認を促す。`

const TEMPLATES: Record<InterviewType, string> = {
  choki: TEMPLATE_CHOKI,
  kostress: TEMPLATE_KOSTRESS,
  fukushoku: TEMPLATE_FUKUSHOKU,
  other: TEMPLATE_OTHER,
}

export function buildSangyoiSystem(type: InterviewType): string {
  return `${COMMON_RULES}\n\n${TEMPLATES[type]}`
}

// 聞き漏らしチェック: 報告書作成に必須の項目リスト(種別ごと)
const CHECKLISTS: Record<InterviewType, string[]> = {
  choki: [
    '直近の時間外労働時間(数値)',
    '睡眠時間・質',
    '疲労蓄積の自覚症状',
    '既往歴・服薬',
    '業務内容と負荷要因',
    '本人の措置への意向',
  ],
  kostress: [
    'ストレス要因(業務量/人間関係/裁量)',
    '心身の症状',
    '睡眠',
    '食欲',
    '受診状況',
    '本人の希望',
  ],
  fukushoku: [
    '現在の症状と安定性',
    '生活リズム',
    '通勤可否',
    '主治医の意見の有無',
    '本人の復職意欲',
    '段階的復帰の要否',
  ],
  other: ['現在の体調', '勤務状況', '通院・服薬状況', '本人の希望'],
}

export function buildCheckSystem(type: InterviewType): string {
  const list = CHECKLISTS[type].map((item) => `- ${item}`).join('\n')
  return `あなたは産業医面談の聞き漏らし防止アシスタントです。面談メモを読み、報告書作成に必須の項目のうち「メモにまだ含まれていない、または情報が不十分な項目」だけを返します。

# 必須項目リスト(面談種別: ${INTERVIEW_TYPE_LABELS[type]})
${list}

# 出力ルール
- 不足項目のみを「・項目名」の箇条書きで返す。1項目1行、補足は書かない。
- メモに少しでも具体的な言及があれば「含まれている」と判定する(厳しくしすぎない)。
- すべて揃っていれば「✓ 必須項目はすべて記録されています」とだけ返す。
- 前置き・後書き・説明は一切書かない。`
}

export function buildCheckUser(type: InterviewType, segments: Segment[]): string {
  const memo = segments
    .filter((s) => s.text.trim())
    .map((s) => s.text.trim())
    .join('\n')
  return `# 面談種別\n${INTERVIEW_TYPE_LABELS[type]}\n\n# ここまでの面談メモ\n${memo}\n\n不足している必須項目を返してください。`
}

export interface SangyoiUserInput {
  type: InterviewType
  caseId: string
  dateStr: string
  durationSec: number
  segments: Segment[]
  judgment?: Judgment
}

function judgmentLines(j: Judgment): string | null {
  const lines: string[] = []
  if (j.workClass) {
    lines.push(`- 判定区分: ${WORK_CLASS_LABELS[j.workClass]}`)
  }
  if (j.measures.length > 0) {
    const parts = j.measures.map((key) => {
      if (key === 'overtime') {
        return `時間外労働の制限(${j.overtimeLimitH.trim() || '　'}時間/月まで)`
      }
      if (key === 'other') {
        return `その他(${j.measureOther.trim() || '内容未記入'})`
      }
      return MEASURES.find((m) => m.key === key)?.label ?? key
    })
    lines.push(`- 措置内容: ${parts.join('、')}`)
  }
  const period = j.period === 'custom' ? j.periodCustom.trim() : j.period
  if (period) {
    lines.push(`- 措置期間: ${period}`)
  }
  return lines.length > 0 ? lines.join('\n') : null
}

export function formatDuration(sec: number): string {
  if (sec <= 0) return '未計測'
  const min = Math.max(1, Math.round(sec / 60))
  return `${min}分`
}

export function buildSangyoiUser(input: SangyoiUserInput): string {
  const lines = input.segments
    .filter((s) => s.text.trim())
    .map((s) => {
      const d = new Date(s.ts)
      const hh = String(d.getHours()).padStart(2, '0')
      const mm = String(d.getMinutes()).padStart(2, '0')
      return `[${hh}:${mm}] ${s.text.trim()}`
    })
    .join('\n')

  const jl = input.judgment ? judgmentLines(input.judgment) : null
  const judgmentBlock = jl
    ? `# 就業判定(産業医が画面で確定入力した判断)
${jl}

この判定は確定事項として扱い、文書の該当欄(就業区分・措置・期間)に必ず反映して対応する選択肢を ☒ にすること。テンプレートに同名の選択肢がない措置は「その他」や具体的内容欄に記載する。`
    : `# 就業判定
構造化入力なし。判断項目はメモから明確に読み取れる場合のみ選択し、曖昧なら空欄(すべて☐)のままにする。`

  return `# 面談情報
- 面談種別: ${INTERVIEW_TYPE_LABELS[input.type]}
- 実施日: ${input.dateStr}
- 面談時間: ${formatDuration(input.durationSec)}
- 対象者ID: ${input.caseId || '(未入力)'}

${judgmentBlock}

# 面談メモ(時刻順セグメント)
${lines}

上記から文書の下書きを作成してください。`
}
